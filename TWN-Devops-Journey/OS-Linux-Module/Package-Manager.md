# How to install software on linux

## what is a software package
## what is a package manager

APT - advanced package tool

manage package with APT
apt search [package_name]
apt search openjdk - for example

apt install [package_name] = install a given package
apt install [package_name_1] [package_name_2] install multiple packages

sudo apt install openjdk-17-jre-headless for eample

apt remove [package_name] = Remove installed package

## importance of package manager

## Different between apt and apt-get

sudo apt update

## Alternative
- Ubuntu software center 
- snap Package Manager - snappy
  - How is snap different
  - Different between snap and apt
- Add repository 
  - PPA = personal package archive

Different linux ditribution and package manager
Debian - apt
Red Hat - yum